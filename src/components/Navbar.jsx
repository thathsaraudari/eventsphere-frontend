import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar">
        <div>
          <button>Login</button>
          <button>SignUp</button>
      </div>
    </nav>
  );
};

export default Navbar;